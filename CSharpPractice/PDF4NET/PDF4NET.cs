using System;
using System.Collections.Generic;
using System.IO;
using System.Text;
using O2S.Components.PDFRender4NET.Printing;
using O2S.Components.PDFRender4NET.WPF;
     

namespace CSharpPractice.PDF4NET
{
    class PDF4NET
    {
         
        public void DoPrint(byte[] Data)
        {
            //PDFPrintSettings pdfPrintSettings = new PDFPrintSettings();
            //using(var stream = new MemoryStream(Data))
            //{
            //    using(var PDF = PDFFile.Open(stream))
            //    {
            //        PDF.Print(pdfPrintSettings);
            //    }
            //}

        }

    }
}
